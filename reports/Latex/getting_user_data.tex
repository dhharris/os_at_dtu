\documentclass{article}
\usepackage[linesnumbered, ruled]{algorithm2e}

\SetKwProg{Fn}{Function}{}{}

\begin{document}
\begin{algorithm}[h] 
\caption{Getting and Executing Commands}
collection $\leftarrow$ initialize to empty Collection\;
running $\leftarrow$ true\;

\While{running is true $\land$ read character is not EOF}{
    status $\leftarrow$ parse\_character($Collection, c$)\;
    \If{status is true}{
      $break$\;
    }
}
\end{algorithm}
\end{document}